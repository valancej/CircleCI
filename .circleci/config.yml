version: 2
jobs:
  build:
    docker:
      - image: anchore/engine-cli:latest
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Build and push Docker image
          command: |
            docker build -t jvalance/sampledockerfiles